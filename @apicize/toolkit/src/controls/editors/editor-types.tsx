export enum RequestEditSessionType {
    Body = 'b',
    Test = 't',
    Setup = 's',
}

export enum ResultEditSessionType {
    Preview = 'p',
    PreviewRequest = 'pr',
    Raw = 'r',
    Base64 = 'b',
    Details = 'd',
    Curl = 'c'
}